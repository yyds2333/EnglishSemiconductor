# WordPin 项目状态

更新时间：2026-08-25

## 当前阶段

v0.1.0 已发布；v0.2.0 本地释义管理与大模型补全正在实现

## 已完成

- 克隆并连接远程仓库 `https://github.com/yyds2333/EnglishSemiconductor.git`。
- 确认远程仓库初始为空仓库。
- 确定只支持 Windows 11 x64。
- 确定最低系统基线为 Windows 11 24H2（OS build 26100）。
- 建立项目规划、规格和 ADR 文档目录。
- 添加基础 Git 忽略、编辑器配置和 .NET SDK 固定配置。
- 创建 .NET 10 + WPF + SQLite 分层工程骨架。
- 完成 Windows 11 x64 Release 构建验证（0警告、0错误）。
- 完成 `win-x64` 自包含发布验证，产物位于 `artifacts/publish/win-x64`。
- 添加可重复执行的 `tools/build.ps1` 构建/发布脚本。
- 建立 ADR-006 熟练度算法候选基线和 PowerShell 序列模拟器。
- 建立 ADR-003 词典数据源与隐私候选基线：本地 ECDICT + 可选在线补充。
- 完成 ECDICT CSV 流式解析、独立 SQLite 词典库和导入命令的技术验证。
- 添加词典基础设施单元测试，当前 3/3 通过。
- 建立 ADR-004 安装/升级/数据目录候选基线和 ADR-005 词单模型基线。
- 完成学习库 SQLite v1 迁移、WAL/外键/busy timeout 配置和基础完整性测试。
- 完成单词、短语、词形、释义、遇见、复习事件、会话和标签表结构。
- 实现大小写/Unicode/空白规范化、立即本地收词和同形词候选保护。
- 接入 WPF 主窗口的主动剪贴板读取、锁定重试、输入框和本地保存；当前全套测试 9/9 通过。
- 接入 `Ctrl+Shift+D` 全局快捷键；冲突时显示 Windows 错误码并提示更换，不静默失效。
- 实现 5 秒撤销：新收词删除新建项，重复收词只删除最近一次遇见记录。
- 接入本地词典提供器；收词后展示本地释义，缺词时明确显示“未找到本地释义”。
- 完成学习数据库 v3 迁移，支持用户释义、AI 候选来源和每日 AI 调用计数。
- 实现释义仓储和来源优先级：用户编辑、AI 已确认、本地词典、AI 未确认候选。
- 实现主窗口添加/编辑/恢复用户释义的入口和持久化。
- 实现应用内 CSV 导入，使用 staging 数据库、备份和原子替换。
- 实现 OpenAI 兼容 HTTPS API 适配器、结构化 JSON 校验、超时和 Bearer Key。
- 实现 DPAPI 加密的 AI 设置、每日请求上限、同词并发去重和 24 小时候选缓存。
- 新增释义、CSV 导入、AI API 和调用限额测试；当前全套自动化测试 19/19 通过。
- 将 ADR-006 确定性规则实现为领域算法和复习事件事务，窗口提供 AGAIN/HARD/GOOD/EASY 反馈；相关测试已纳入全套 19/19 测试。
- 实现按本地日期固定的今日队列快照和互斥分类，窗口启动时显示今日队列数量；相关测试已纳入全套 19/19 测试。
- 加入 Inno Setup x64 per-user 安装脚本和 `build.ps1 -Installer` 入口；v0.1.0 安装包已生成并发布。

## 当前 P0 决策

- 熟练度算法序列模拟和不变量测试已通过，进入实现验收。
- 指定 ECDICT 版本的许可和数据来源人工复核仍需在实际分发词典前完成。
- v0.2.0 的首个实际大模型服务和默认模型名称仍需确定；代码保持 OpenAI 兼容接口。
- 安装包尚未配置代码签名证书，SmartScreen 可能显示未知发布者。

## 下一步 READY 任务

1. 完成 v0.2.0 端到端 Windows 11 x64 手工验收。
2. 增加 AI 候选的单独“采用/忽略/重新生成”按钮和更细的来源展示。
3. 增加同形词选择、队列项目完成状态和进度统计。
4. 增加托盘、单实例和启动后置顶行为。
5. 复核 ECDICT 许可并确定代码签名证书，发布 v0.2.0 安装包。

算法模拟命令：

```powershell
.\tools\simulate-mastery.ps1
```

## 发布规则

v0.1.0 已满足首个安装包发布准入并发布到 GitHub Release。v0.2.0 仍需通过新增功能的 Windows 11 x64 回归、升级和隐私验收后再发布。

## 当前阶段备注

工程骨架、词典基础设施、学习库、主动收词界面、全局快捷键、撤销、本地释义、熟练度反馈、今日队列、释义编辑、CSV 导入和 AI API 适配器已通过构建验证；下一步完成 v0.2.0 交互细化和发布验收。
